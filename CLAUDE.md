# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## О проекте

> Проект переориентирован 30.07.2026 с цикла продления прав MDPH на сопровождение
> страхового случая catastrophe naturelle — причины и проверка источников данных в
> `docs/decisions.md`, запись «пивот» от 30.07.2026.

**Mon Sinistre** — assistant catastrophe naturelle: уведомляет жителей выбранных коммун в день публикации arrêté в Journal Officiel и ведёт пострадавшего через страховой случай — план действий с первого дня после события, 30-дневный срок декларации, сроки страховщика, инвентарь ущерба с фото. Оно не даёт юридических консультаций и не взаимодействует со страховщиком от имени пользователя — его задача не дать пропустить сроки.

## Карта документации

Один факт живёт в одном файле. Кто за что отвечает:

| Документ                          | Отвечает за                                                                                                             |
| --------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `docs/technical-specification.md` | что продукт делает и чего не делает — требования продукта, единственный источник                                        |
| `docs/decisions.md`               | почему решили так и что отвергли; история и инциденты                                                                   |
| `docs/prd/<фича>.md`              | требования фичи                                                                                                         |
| `docs/plan/<фича>.md`             | разбивка фичи на фазы                                                                                                   |
| `docs/research/<фича>.md`         | технические решения фичи и «как применять» — **снапшот на дату**; расхождения с реализацией отмечаются врезкой в тексте |
| корневой `CLAUDE.md`              | навигация, механика монорепозитория, выжимка жёстких ограничений                                                        |
| `apps/*/CLAUDE.md`                | стек, команды и правила уровня приложения                                                                               |
| `apps/api/src/<модуль>/CLAUDE.md` | устройство и правила конкретного модуля                                                                                 |

ТЗ определяет функциональные требования, но не контракты API и не структуру
модулей — эти решения принимаются в коде.

**Файлы `CLAUDE.md` вложены, а не наследуются:** корневой загружен всегда, файл
приложения — дополнительно к нему, модульный — дополнительно к обоим. Поэтому
факт, написанный на уровне выше, ниже не повторяется: копия не добавляет
контекста, а разойтись с оригиналом успевает. Исключение ровно одно и оно
осознанно — «Жёсткие ограничения» ниже кешируют ТЗ § 7.

## Структура монорепозитория

npm workspaces, три пакета:

- `packages/contracts` (`@mon-sinistre/contracts`) — типы, enum'ы и константы, общие для API и клиента. Собирается **первым**: API импортирует его из `dist/`.
- `apps/api` (`@mon-sinistre/api`) — NestJS + Fastify + Prisma + PostgreSQL, порт 3001.
- `apps/web` (`@mon-sinistre/web`) — Next.js (App Router) + Tailwind 4, порт 3000. Contracts резолвится в его `dist/` (поле `main`), поэтому web тоже требует собранный contracts; `transpilePackages` нужен, чтобы dev-сервер подхватывал пересборку пакета.

У каждого приложения есть свой CLAUDE.md; у модулей `apps/api/src/` со своими
правилами (`prisma`, `communes`, `mail`) — тоже.

## Команды (из корня)

```bash
npm run db:up            # Postgres 18 в Docker, читает apps/api/.env
npm run build:contracts  # обязательно перед первым запуском API и web
npm run dev:api          # http://localhost:3001, /docs — OpenAPI
npm run dev:web          # http://localhost:3000
npm run dev:contracts    # tsc --watch, при параллельной правке типов
npm run build            # порядок обязателен: contracts → api → web
npm test                 # доменные тесты есть только в apps/api
npm run test:tooling     # .claude/ — почему отдельно, абзац ниже
npm run lint             # по всем workspace'ам
```

`.claude/` не входит в workspaces, поэтому ни eslint, ни jest его не видят — для скриптов обвязки заведён отдельный `test:tooling` на встроенном `node --test` (без зависимостей, Node ≥ 24 уже требуется). Новый файл рядом с `.claude/*.test.js` подхватывается сам.

Pre-commit хук (`.githooks/pre-commit`, подключается через `core.hooksPath` скриптом `prepare` при `npm install`) прогоняет `npm run lint`, `npm test` и `npm run test:tooling` перед каждым коммитом. Хук не должен изменять файлы — скрипты `lint` запускаются без `--fix` (в `@mon-sinistre/api` для автопочинки есть отдельный `lint:fix`).

Окружение: Node.js ≥ 24, Docker. Секреты — `cp apps/api/.env.example apps/api/.env`, значения генерируются `openssl rand -base64 48`.

## Ralph Loop — автономный прогон фазы

```bash
node .claude/ralph.js --dry-run   # проверить обвязку, не запуская агента
node .claude/ralph.js             # прогнать фазу из .claude/ralph.config.json
```

Цикл берёт открытые issues milestone по возрастанию номера и на каждый запускает
отдельную сессию `claude -p` с чистым контекстом. Правила итерации —
`.claude/ralph.md` (в интерактивных сессиях не действуют). Настройки —
`.claude/ralph.config.json`: фича, номера фаз, лимиты, права.

Цикл сам делает то, чего не делает агент: сверяет ветку и коммит итерации
(ровно один, с закрывающим трейлером), пушит, переводит issue в In Review,
открывает PR после закрытия фазы. Имя ветки выводится как `{фича}/phase-{N}`;
milestone ищется по формату `[{фича}] Фаза N: …`, который задаёт скилл `issues`.
Несколько фаз проходятся только последовательно — ветка каждой растёт от
`origin/main`, и следующая начнётся, лишь когда PR предыдущей смержен.

Предполётно требуются `docs/prd/{фича}.md`, `docs/plan/{фича}.md`,
`docs/research/{фича}.md` и чистое рабочее дерево. Условия останова и причины
каждой проверки — в `.claude/ralph.js` и `.claude/ralph.md`; общий принцип: цикл
останавливается на всём, что молча потеряло бы работу или выбросило issue из
очереди.

Права агента намеренно узкие: `allowedTools` — allowlist, а не «всё, кроме».
Состав списка и его обоснование — `.claude/ralph.md`, раздел «Завершение
сессии». Новая зависимость в фазе — работа для человека, не для цикла.

## Коммиты и PR

Живой номер в закрывающем трейлере — **ровно один**: тот, что относится к этому
коммиту. GitHub ищет ключевые слова (`Closes`, `Fixes`, `Resolves` и формы) по
всему тексту сообщения коммита, заголовка и тела PR и оформление не разбирает —
кавычки и бэктики не спасают.

Поэтому: примеры и объяснения трейлера — только с плейсхолдером (`Closes #<N>`);
описывая инцидент, ключевое слово и живой номер рядом не ставить — писать
«issue 12», а не трейлер с этим номером. Цена нарушения и как оно уже дважды
случилось — `docs/decisions.md`, запись о закрывающем трейлере от 03.08.2026.

## Архитектура и доменная модель

Сущности и их отношения — ТЗ § 4; реляционная схема — `docs/research/data-model.md`
(связи, ключи, каскады, версионирование справочников, идемпотентность рассылок,
RGPD-удаление). Prisma-модели и миграции обязаны следовать модели; осознанное
отступление — через запись в `docs/decisions.md` и правку модели, не молча.

Правила, которые из ТЗ не выводятся и живут в коде:

- **План разворачивается от опорных дат** (`StepAnchor`). Шаг с ещё не
  наступившим якорем существует без `plannedDate` и получает дату, когда якорь
  появляется — так синистр, созданный до публикации arrêté, автоматически
  получает 30-дневный дедлайн в момент, когда монитор находит arrêté.
- **Статусы шагов вычисляются на чтении**, в базе хранятся только `FAIT` и
  `NON_APPLICABLE`. Пороги (`SOON_THRESHOLD_DAYS = 30`, напоминания 30/14/3,
  усиленная шкала дедлайна декларации 21/14/7/3/1) — константы contracts, не
  дублировать в приложениях.
- Синистр хранит **копию плана** на момент создания: изменение шаблона не
  трогает существующие синистры. Шаги, добавленные пользователем
  (`fromTemplate: false`), никогда не пересчитываются.
- Все доменные даты — `IsoDate` (брендированная строка `YYYY-MM-DD`, без времени
  и таймзон; произвольный `string` не пройдёт типизацию — конструировать через
  `toIsoDate`/`isIsoDate` из contracts). Не заменять на `Date`.

## Жёсткие ограничения (из ТЗ, нарушать нельзя)

Осознанный кеш ТЗ § 7 для контекста сессий: правится вслед за ТЗ, не вместо
него. В `CLAUDE.md` приложений и модулей эти пункты **не повторяются** — этот
файл загружен всегда.

- **Доступность — WCAG 2.1 AA / RGAA 4.1.2** (§ 7 ТЗ) — обязательное требование наравне с функциональными: аудитория — люди в остром стрессе после катастрофы, значительная доля пожилых. Публикуемая déclaration d'accessibilité — часть продукта.
- **Файлы пользователей приватны**: фото и justificatifs — только по коротким подписанным URL, публичных бакетов нет.
- Проверка принадлежности объектов пользователю выполняется **в запросе к базе**, а не после выборки; ответ на чужой объект не раскрывает факт его существования.
- Язык интерфейса — **французский**; все пользовательские строки — в файлах локализации, contracts не содержит текстов для UI.
- Изменения схемы БД — только через `prisma migrate`; `prisma db push` в продакшене запрещён.
- В логи не попадают email, адреса, содержимое инвентаря и файлы.
- Email наблюдателей (Veille): double opt-in, отписка в один клик, немедленное удаление по запросу (RGPD).
- **Юридические сроки — только из справочника `DeadlineRule`** с `SourceReference`; захардкоженных юридических цифр в коде нет.
- **Дата публикации arrêté — только из XML JORF**; дата появления файла в выгрузке DILA и даты из GASPAR источниками не являются (GASPAR ошибается, проверено 30.07.2026).
- Справочные данные всегда несут ссылку на источник и дату сверки (`SourceReference`); при расхождении сведений применяется более консервативное значение; при расхождении GASPAR и JORF приоритет у JORF.

## Тестирование

Обязательное покрытие задаёт **ТЗ § 9** — состав не сокращать: парсер arrêté на
фикстурах реальных XML из JORF, вычисление дедлайнов, статусы шагов и шаги без
`plannedDate`, поводы для напоминаний и их идемпотентность, дедупликация NOR,
переходы статуса при отказе. Обязательные краевые случаи каждого пункта
перечислены там же.

## Актуализация документации

При изменении архитектуры обновлять документацию в том же коммите, что и код,
по «Карте документации» выше:

- новый пакет в workspace, изменение порядка сборки или команд — этот файл и README.md;
- новые модули, изменение состояния скелета, новые правила приложения — CLAUDE.md соответствующего приложения или модуля;
- отступление от ТЗ или уточнение требований — зафиксировать решение и его причину в журнале `docs/decisions.md`, само ТЗ не редактировать молча;
- расхождение реализации с research-отчётом — врезкой в самом отчёте («Исправлено при реализации: …») со ссылкой на запись журнала: research читается как инструкция, в том числе Ralph Loop.

Один и тот же факт, изложенный в двух файлах без пометки источника, — баг
документации. Устаревшая инструкция хуже отсутствующей: следующая сессия будет
ей доверять.
