/**
 * Contracts shared by the API and the web client.
 *
 * This package holds types, enums and constants only — no runtime dependencies,
 * no framework code, and no user-facing French strings. Anything displayed to a
 * user belongs in the web client's locale files.
 */

export * from './enums';
export * from './models';
